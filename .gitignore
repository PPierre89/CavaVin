venv/
__pycache__/
*.pyc
backend/db.sqlite3
backend/staticfiles/
# SPA React buildé (copié dans l'image par le Dockerfile ; jamais commité)
backend/spa/
# Bind mounts créés par docker-compose (SQLite + statiques) si lancé depuis le repo.
/data/
/staticfiles_data/
.env
*.log
# Rapports de couverture de test (générés en CI / local)
.coverage
backend/htmlcov/
backend/coverage.xml
